const path = require("path")

module.exports = {
  resolve: {
    fallback: {
      "crypto": false
    }
  },
  entry: path.resolve(__dirname, "src/index_webpack.js"),
  output: {
    path: path.resolve(__dirname, "dist"),
    filename: "main.js",
    library: "AdCypressLib",
    libraryTarget: "umd",
  },
  module: {
    rules: [
      {
        test: /\.(js)$/,
        exclude: /node_modules/,
        use: "babel-loader",
      },
    ],
  },
  mode: "production",
}
